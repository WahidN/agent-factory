## 0. Housekeeping

- [x] 0.1 Archive `add-network-hub` with `openspec archive`, so the main `factory-scene` spec holds the version this change's deltas for "One factory per session" and "Hover details" copy; verify `openspec validate add-token-milestones --strict` still passes

## 1. Server: count tokens

- [x] 1.1 In `server/claude-reader.ts` add the `usage` transcript event (`id` as `message.id|requestId`, falling back to the line `uuid` when both are empty, `total` as the sum of the four usage fields, `at`) to `parseTranscriptChunk`, and add `parseUsageLine(line)` that returns the same for one line after a `"usage"` and `"assistant"` substring check; add the fixture `server/tests/fixtures/usage.jsonl` with a message logged twice with growing output, an assistant line without usage, a user line and a `<synthetic>` line; verify Vitest tests pass for both functions on that fixture
- [x] 1.2 Create `server/usage-ledger.ts` with `UsageLedger` (`add(id, total)` returns whether the sum changed, `total()`), keeping the largest total per id; verify Vitest tests pass for a new id, a smaller duplicate that changes nothing, a larger duplicate that adds only the difference, and the total across ids
- [x] 1.3 In `server/types.ts` add `machineTokens: number` to `SessionState`; in `server/session-tracker.ts` add `setMachineTokens(total, now)` that stores the number, puts it on every session in `stateOf`, and emits every session; verify Vitest tests pass for the field on a new session (0), on every session after `setMachineTokens`, one update per session on a change, and no update when the number is the same
- [x] 1.4 In `server/index.ts` split tail events so `usage` goes to the ledger and the rest to the tracker, call `setMachineTokens` when the ledger total changed, add `scanUsage()` that walks `~/.claude/projects/` for `.jsonl` files after the first `refreshSessions` and streams each file line by line through `parseUsageLine` into the ledger, publishing the total once when the scan is done, and give a transcript first seen from the middle a background full read of that one file; in `server/hub.ts` set `PROTOCOL` to 2; verify with `pnpm dev` that the log shows the scan finishing in a few seconds, that a browser snapshot carries `machineTokens` equal to the total of the script in the design context, and that a new assistant turn in a running session raises it
- [x] 1.5 In `server/usage-ledger.ts` export `SEASON_START` as `Date.parse("2026-09-16T00:00:00Z")`, give `UsageLedger` a constructor argument `since` and an `at` argument on `add` that returns false for anything older; in `server/index.ts` construct the ledger with `SEASON_START` and pass the event's `at` at both call sites; verify Vitest tests pass for a message before the start (ignored, total stays 0), one exactly at the start (counted), and the existing cases with `since` 0, and that `pnpm test` and `pnpm typecheck` pass

## 2. Web: quiet by default

- [x] 2.1 In `web/park-layout.ts` change `movingCarCount` to `lotBusy ? Math.min(6, busySubagents) : 0` and update its tests for idle, busy without subagents, 3 and 8 busy subagents; in `web/machines.ts` move the truck's part list into `addTruckParts(b)` used by `createTruck`; in `web/traffic.ts` give `addParkedCars` a `count` argument, 0 to 3, that keeps the bay lines and the color hash; in `web/lot.ts` remove the parked truck clone from the constructor and the default parked cars from `buildStructure`; verify Vitest and typecheck pass and that in agent-browser an idle lot shows bay lines only and sends no cars, and a busy lot sends its truck

## 3. Web: token milestones

- [x] 3.1 Create `web/milestones.ts` with `MILESTONES` (10M, 25M, 50M, 100M, 250M, 500M, 750M, 1B, 2.5B, 5B), `milestoneIndex(tokens)` and the `buildMilestones(builder, count, ctx)` entry point with an empty row table; verify Vitest tests pass for `milestoneIndex` at 0, at each threshold, one token below each threshold, and far above 5B
- [x] 3.2 Add the builders for rows 1 to 7 at the positions in the design placement table: bike rack with 3 bikes, the 3 parked cars through `addParkedCars` with the count from the row, the flagpole with an accent flag, the coffee cart and picnic table, the truck through `addTruckParts`, and the 2 EV chargers with a sports car; verify in agent-browser with a 600M fixture that every piece sits inside the yard, clear of the hall, machines, warehouse slots and worker routes on the small and the huge tier
- [x] 3.3 Add the helipad with a white H and the helicopter (body, tail, skids, main rotor, tail rotor) for row 8 on the hall roof, in the back left corner; verify in agent-browser with 1B fixtures on the small and the medium hall that the pad and the helicopter stay clear of the corner vent, the skylight, the roof letters and the parapet
- [x] 3.4 Add the wind turbine and the blimp as `Animated` objects with a `group` and `tick(dt)`: the turbine's 3 blades turn in a plane facing +z, the blimp bobs 0.6 over 6 s on a tether above the hall center, both disposed with the structure; verify in agent-browser with a 5B fixture that `getBoundingClientRect` samples of the blimp change over 3 seconds while the lot is idle, and that the blades turn
- [x] 3.5 In `web/lot.ts` pass the accent material, the hall footprint and the session id in `MilestoneContext`, call `buildMilestones` from `buildStructure`, keep `builtMilestone` and rebuild when `milestoneIndex(state.machineTokens)` differs, and tick the animated extras from `tick`; verify in agent-browser that raising a fixture total from 99M to 101M adds the 3rd car and the flagpole without the lot sinking and with its warehouses in place

## 4. Web: tooltip

- [x] 4.1 In `web/sign-text.ts` add `shortTokens(n)` (units k, M, B, 1 decimal below 10 of a unit, none above, no trailing `.0`) with Vitest tests for 950, 12k, 2.5M, 408M, 4.4B and 25B; in `web/tooltip.ts` add a `tokens` line with `shortTokens(machineTokens) + " tokens"` when the hovered state has `machineTokens`, and include it in the render key; verify in agent-browser that hovering a hall shows the line and hovering a warehouse does not

## 5. Docs

- [x] 5.1 In `README.md` update the state table so idle sends no vehicles and busy sends the truck plus one car per busy subagent, and add a "Token milestones" section with the ladder table, the counting rule (all four token kinds of every assistant message in `~/.claude/projects/`, each message once, same as PokeTokenBar and ccusage), the note that the total is 0 until the scan at start is done, and that on a hub every machine's total is visible to everyone on it
- [x] 5.2 In `README.md` add the season start to the counting rule and the intro line: messages from before 16 September 2026 00:00 UTC add nothing, so every machine starts at 0 on the same day, and a new season is one date change; replace "lifetime" where the README says it; verify the section reads that way

## 6. End-to-end check

- [x] 6.1 Point `HOME` at a scratch folder holding a copy of `server/tests/fixtures/session.json` with a live pid and a transcript with one assistant line of 5B tokens; verify in agent-browser that the lot shows every extra, the tooltip reads `5B tokens`, the turbine turns and the blimp bobs; repeat with 30M (bike rack and 1 car only) and with a transcript without usage (bay lines only, no road cars while idle)
- [x] 6.2 Run `pnpm test`, `pnpm typecheck` and `openspec validate add-token-milestones --strict`; verify all pass, and take a screenshot of a 5B lot for the README
